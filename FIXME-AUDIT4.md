# Аудит 4-р тойрог — 5105 дансны регресс

> 3-р тойргийн засвар **бүрэн зөв**: дахин үнэлгээ өмчид (4104) буусан,
> бууралт нь ч журналд орж байна, GL-ийн тест 4 ширхэг нэмэгдсэн (98/98 ногоон).
>
> Гэвч `5105` дансны **нэрийг өөрчлөхөд түүнийг ашигладаг 2 модуль
> шинэчлэгдээгүй** тул шинэ алдаа үүссэн. Доор ганц асуудал байна.

---

## 1. `5105` данс — нэр нь утгаасаа салсан

`coa_seed.py`-д `5105` одоо ийм байна:

```python
("5105", "Урьдчилж орсон орлого", "credit", True, "51"),   # ← 51 = Борлуулалтын орлого
```

Хоёр асуудал:

**а) Ангилал буруу.** «Урьдчилж орсон орлого» бол **өр төлбөр** (харилцагчаас
урьдчилж авсан мөнгө, үйлчилгээгээ хараахан үзүүлээгүй). Одоо энэ нь
`51 Борлуулалтын орлого`-ийн доор байгаа тул `reports.py`-ийн
`_sum_by_prefix(tb, ("51",))` үүнийг **орлого гэж тоолно** — СТ-2-ын борлуулалт
хөөрөгдөж, СТ-1-ийн өр төлбөр дутуу харагдана.

**б) Давхардсан.** `3108 "Урьдчилж орсон орлого"` аль хэдийн байгаа бөгөөд тэр нь
зөв ангилалтай (`31` өр төлбөрийн доор). Одоо ижил нэртэй хоёр данс тайлангийн
хоёр өөр хэсэгт байна — нябо алийг нь сонгохоо мэдэхгүй.

## 2. Нэр солигдсоны улмаас 2 модуль буруу дансанд бичиж байна

`5105`-ыг өмнө нь «Бусад орлого» гэж үзэж хоёр газар ашиглаж байсан. Кодын
тайлбар нь одоо ч тэгж бичсэн хэвээр:

| Файл | Мөр | Одоогийн код | Тайлбарт юу гэж бичсэн | **Байх ёстой** |
|---|---|---|---|---|
| `src/bayan/fx.py` | 69 | `code == "5105"` | «олз: 5105» | **`5204`** Ханшийн зөрүүгийн олз |
| `src/bayan/fx.py` | 70 | `code == "7199"` | «гарз: 7199» | **`7118`** Ханшийн зөрүүгийн гарз |
| `src/bayan/inventory.py` | 456 | `credit_account="5105"` | «Kt 5105 (Бусад орлого)» | **`5201`** Бусад орлого |

`fx.py`-ийн 73-р мөрийн алдааны мессежийг ч мөн шинэчилнэ:
`"Ханшийн зөрүүний олз (5204) эсвэл гарз (7118) данс тохируулагдаагүй байна."`

### ⚠️ Мөн энэ нь модуль хоорондын зөрчил үүсгэж байна

Ижил үйл явдал — ханшийн зөрүү — хоёр өөр дансанд бичигдэж байна:

- `api.py` (3-р тойрогт зассан): олз **5204**, гарз **7118** ✓
- `fx.py` (зассангүй): олз **5105**, гарз **7199** ✗

Аль код замаар явснаас хамаарч ханшийн зөрүү өөр дансанд буух нь тайланг
эвдэнэ. Хоёуланг нь `5204`/`7118` болгож нэгтгэнэ.

## 3. `5105`-ыг юу хийх вэ

`fx.py`, `inventory.py` хоёрыг дээрх байдлаар зассаны дараа `5105` хэрэглэгчгүй
үлдэнэ. Хамгийн цэвэр нь **SEED-ээс бүрмөсөн хасах** — «урьдчилж орсон орлого»-г
`3108` аль хэдийн зөв ангилалтайгаар хангаж байгаа:

```python
# coa_seed.py — энэ мөрийг УСТГА:
("5105", "Урьдчилж орсон орлого", "credit", True, "51"),
```

Устгасны дараа `5105` хаана ч үлдээгүйг шалга:

```bash
grep -rn "5105" src/ scripts/ tests/
```

`scripts/demo.py:58`-д `("урьдчилгаа", "5105", 20)` гэсэн ангилалтын дүрэм
байгаа — үүнийг `"3108"` болгож солино (харилцагчийн урьдчилгаа = өр төлбөр).

---

## 4. Шалгалт

```bash
cd D:\mcp\bayan-ai
.venv\Scripts\python.exe -m pytest tests\ -q
```

Ханшийн зөрүү зөв дансанд буусныг батал — `fx-revalue` дуудаад гүйлгээ
балансад `5204` эсвэл `7118` хөдөлсөн, `5105` огт байхгүй байх ёстой.

Мөн дансны төлөвлөгөөнд ижил нэртэй данс давхардаагүйг шалга:

```bash
.venv\Scripts\python.exe -X utf8 -c "import sys; sys.path.insert(0,'src'); ^
from bayan.coa_seed import SEED; from collections import Counter; ^
n=Counter(x[1] for x in SEED); ^
print('давхардсан нэр:', {k:v for k,v in n.items() if v>1} or 'алга')"
```

---

## 5. Үлдсэн (хойшлуулж болох, өмнөх тойргуудаас)

- `JournalLine.amount_currency` — мөнгө `Float` хэвээр, `fx.py`-д `SUM()` хийгддэг
- Endpoint дотор `db.commit()` 42 газар — `get_db()` аль хэдийн commit хийдэг
- `api.py` 5,900 орчим мөр, 204 endpoint — router болгон салгах
- **`git init`** — код одоо хамгийн сайн байдалдаа, эхний commit хийх хамгийн зөв цаг
